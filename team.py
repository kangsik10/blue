# 평균을 구하는 함수
def f_avg(data):
    print("1")
    return 1

# 합계를 구하는 함수
def f_sum(data):
    print("2")
    return 1

# 오름차순으로 정렬해주는 함수
def f_sort(data):
    sorted_data = sorted(data)
    return sorted_data

# 내림차순으로 정렬해주는 함수
def f_desc(data):
    sorted_data = sorted(data, reverse=True)
    return sorted_data